dcmNetworkConnection:网络连接扩展|dcm4che专有网络连接属性
dcmNetworkConnection.dcmProtocol:协议|网络连接协议。
dcmNetworkConnection.dcmHTTPProxy:HTTP代理|HTTP代理:[user:password@]host:port
dcmNetworkConnection.dcmTLSNeedClientAuth:TLS需要客户端验证|指示是否需要TLS客户端验证。
dcmNetworkConnection.dcmTLSProtocol:TLS协议|支持的TLS协议。
dcmNetworkConnection.dcmTLSEndpointIdentificationAlgorithm:TLS Endpoint Identification Algorithm|Indicates the endpoint identification or verification procedures during TLS handshaking.
dcmNetworkConnection.dcmTCPBacklog:TCP积压|传入TCP连接的最大队列长度。
dcmNetworkConnection.dcmTCPConnectTimeout:TCP连接超时|TCP连接超时（以毫秒为单位）；如果不存在,则没有超时。
dcmNetworkConnection.dcmTCPCloseDelay:TCP关闭延迟|发送A-ASSOCIATE-RJ,A-RELEASE-RP或A-ABORT PDU后的TCP套接字关闭延迟（毫秒）。
dcmNetworkConnection.dcmTCPSendBufferSize:TCP发送缓冲区大小|TCP发送缓冲区大小；如果不存在,则使用系统默认值。
dcmNetworkConnection.dcmTCPReceiveBufferSize:TCP接收缓冲区大小|TCP接收缓冲区大小；如果不存在,则使用系统默认值。
dcmNetworkConnection.dcmTCPNoDelay:无TCP延迟|启用/禁用TCP_NODELAY（禁用/启用Nagle算法）。
dcmNetworkConnection.dcmBindAddress:绑定地址|监听套接字的绑定地址。如果不存在,则使用连接的主机名。
dcmNetworkConnection.dcmClientBindAddress:客户端绑定地址|传出连接的绑定地址，如果不存在,则使用连接的主机名。
dcmNetworkConnection.dcmBlacklistedHostname:黑名单主机名|黑名单DNS主机名
dcmNetworkConnection.dcmSendPDULength:发送PDU长度|发送PDU的最大长度。
dcmNetworkConnection.dcmReceivePDULength:接收PDU长度|接收PDU的最大长度。
dcmNetworkConnection.dcmMaxOpsPerformed:执行的最大操作数|异步执行的最大操作数。0=无限。
dcmNetworkConnection.dcmMaxOpsInvoked:调用的最大操作数|异步调用的最大操作数。0=无限。
dcmNetworkConnection.dcmPackPDV:Pack PDV|启用/禁用打包命令和数据PDV到单个P-DATA-TF PDU。
dcmNetworkConnection.dcmAARQTimeout:AA-RQ超时|TCP连接后接收A-ASSOCIATE-RQ PDU的超时（毫秒）；如果不存在,则没有超时
dcmNetworkConnection.dcmAAACTimeout:AA-AC超时|在发送A-ASSOCIATE-RQ PDU之后接收A-ASSOCIATE-AC PDU的超时（毫秒）； 如果不存在,则没有超时。
dcmNetworkConnection.dcmARRPTimeout:AR-RP超时|在发送A-RELEASE-RQ PDU之后接收A-RELEASE-RP PDU的超时（毫秒）；如果不存在,则没有超时。
dcmNetworkConnection.dcmSendTimeout:发送超时|发送除C-STORE RQs之外的DIMSE RQs的超时（毫秒）；如果不存在,则没有超时。
dcmNetworkConnection.dcmStoreTimeout:存储超时|发送C-STORE RQ的超时（毫秒）；如果不存在,则没有超时。
dcmNetworkConnection.dcmResponseTimeout:响应超时|<br>- 接收非C-MOVE或C-GET RSPs的未解析DIMSE RSP <br>- responses of outgoing HL7 message <br>- HTTP(S) responses <br>的超时（毫秒）； 如果不存在,则没有超时。
dcmNetworkConnection.dcmRetrieveTimeout:读取超时|接收未解析的C-GET或C-MOVE RSP的超时（毫秒）；如果不存在,则没有超时。
dcmNetworkConnection.dcmRetrieveTimeoutTotal:读取总超时|当接收到挂起的RSP时,指示是重新启动（=false）还是不重新启动（=true）具有未解析的C-GET和C-MOVE RSP指定超时的计时器。
dcmNetworkConnection.dcmIdleTimeout:空闲超时|指示空闲关联在指定的超时（以毫秒为单位）之后停止。如果不存在,则没有超时。
dcmNetworkConnection.dcmAATimeout:A-ABORT Timeout|Timeout in ms for waiting for finishing sending any DIMSE before sending an A-ABORT PDU, triggered by the application or by expiration of a configured other timeout of this Connection. If the timeout expires, the TCP connection will be closed without sending the A-ABORT.
