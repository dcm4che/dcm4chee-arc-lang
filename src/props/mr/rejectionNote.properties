rejectionNote:Rejection Note|Specifies behavior on Rejection Note Stored
rejectionNote.dcmRejectionNoteLabel:Rejection Note Label|Rejection Note Label
rejectionNote.dcmRejectionNoteType:Rejection Note Type|Type of Rejection Note.
rejectionNote.dcmRejectionNoteCode:Rejection Note Code|Specifies Document Title of Rejection Note in format (CV, CSD, 'CM')
rejectionNote.dcmAcceptPreviousRejectedInstance:Accept Previous Rejected Instance|Specifies behavior on subsequent occurrence of instances rejected by a particular Rejection Note.
rejectionNote.dcmOverwritePreviousRejection:Overwrite Previous Rejection|Specifies Document Title of previous Rejection Note in format (CV, CSD, 'CM') which may be overwritten by that Rejection Note
rejectionNote.dcmAcceptRejectionBeforeStorage:Accept Rejection before Storage|Time interval in ISO-8601 duration format PnDTnHnMn.nS after receive of a Rejection Note, in which received object referenced by this Rejection Note are treated as rejected. Referenced objects received afterwards are treated as subsequent occurrence of an already rejected instance. If not present, Rejection Notes which refers not yet received objects will not be accepted.
rejectionNote.dcmDeleteRejectedInstanceDelay:Delete Rejected Instance Delay|Delay in ISO-8601 duration format PnDTnHnMn.nS after which instances rejected by a particular Rejection Note are deleted. Infinite if absent.
rejectionNote.dcmDeleteRejectionNoteDelay:Delete Rejection Note Delay|Delay in ISO-8601 duration format PnDTnHnMn.nS after which particular Rejection Notes are deleted. Infinite if absent.
